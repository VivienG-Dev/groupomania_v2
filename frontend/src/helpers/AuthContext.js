// On va créer une variable "context" qui va permettre de récupérer et rendre...
// ...les informations de l'utilisateur (username, auth...)
import { createContext } from "react";

export const AuthContext = createContext('');